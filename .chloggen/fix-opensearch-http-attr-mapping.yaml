# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: bug_fix

# The name of the component, or a single word describing the area of concern, (e.g. frontend, checkout, collector, load-generator)
component: cart

# A brief description of the change. Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Set the cart listen port via ASPNETCORE_HTTP_PORTS instead of ASPNETCORE_URLS so it stops emitting a scalar `http` log attribute that broke OpenSearch log ingestion

# One or more tracking issues or pull requests related to the change. You can use the PR number here if no issue exists.
issues: [4012]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use a strip-chomping block scalar (|-) for multiline entries so no trailing
# whitespace is emitted into CHANGELOG.md.
subtext: |-
  ASPNETCORE_URLS overrode the framework default HTTP_PORTS, so Kestrel logged
  an "Overriding HTTP_PORTS" warning whose "{http}" template field became a
  scalar `http` log attribute. In OpenSearch ss4o storage that mapped
  attributes.http as text, after which frontend logs carrying the semconv
  http.request.method attribute were rejected with a mapper_parsing_exception,
  dropping the frontend logs and failing the Telemetry Tests.
