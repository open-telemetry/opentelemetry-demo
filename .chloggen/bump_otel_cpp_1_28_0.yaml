# Use this changelog template to create an entry for release notes.

# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: breaking

# The name of the component, or a single word describing the area of concern, (e.g. frontend, checkout, collector, load-generator)
component: currency

# A brief description of the change. Surround your text with quotes ("") if it needs to start with a backtick (`).
note: "Bump `opentelemetry-cpp` to 1.28.0 and migrate the RPC span attributes it deprecates to the current semantic conventions"

# One or more tracking issues or pull requests related to the change. You can use the PR number here if no issue exists.
issues: [3967]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use a strip-chomping block scalar (|-) for multiline entries so no trailing
# whitespace is emitted into CHANGELOG.md.
subtext: |-
  `rpc.system` is replaced by `rpc.system.name`, `rpc.service` is folded into a
  fully-qualified `rpc.method` (for example `oteldemo.CurrencyService/Convert`),
  and the integer `rpc.grpc.status_code` is replaced by the string
  `rpc.response.status_code`.
