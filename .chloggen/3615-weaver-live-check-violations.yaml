change_type: breaking

component: ad, recommendation

note: Fix telemetry that violates the Demo's Weaver registry.

issues: [3615]

subtext: |-
  Rename `demo.ad.context_keys` to `demo.ad.context_keys.values` to avoid a
  namespace collision with `demo.ad.context_keys.count`. Align the
  `demo.recommendation.requests` unit with its schema.
