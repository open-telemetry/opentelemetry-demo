Input: 
- a document about critical user path of the system located at doc/CRITICAL_USER_PATHS_ANALYSIS.md
- Refence at doc/application-overview.md

Output: 
- A documentation about detailed SLO for each critical user path at doc/CRITICAL_USER_PATHS_SLO.md


System Role: You are a Principal Site Reliability Engineer (SRE) and Observability Architect.

Context:I am providing you with a document about critical user path of the system. This document maps out multiple Critical User Paths (CUPs) of the application.

Objective:Based strictly on the detailed endpoints and flows in the analysis, generate a sensible SLO requirement for each criticual user path.

Define specific SLOs for all critical user paths (CUP). For each CUP, provide:
  Availability Target: (e.g., 99.9% success rate).
  Latency Target: (e.g., P95 < 800ms, P99 < 2s). Justify your thresholds based on the complexity described in the analysis (e.g., complex quote calculations vs. simple login).
  SLI Definition: The specific metric formula (e.g., count(http_requests_total{status=5xx}) / count(http_requests_total)).
