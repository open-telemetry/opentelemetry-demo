
Input:
- a document about the critical user paths of the system `doc/CRITICAL_USER_PATHS_ANALYSIS.md`
- SDK instrumentation implementation plan at `doc/CRITICAL_USER_PATHS_SDK_INSTRUMENTATION_PLAN.md`
- application overview at `doc/application-overview.md`

Output:
- Update code for the given application.
- Configuration files updated to support OTLP and environment-driven settings
- Summary of actions taken in `doc/CRITICAL_USER_PATHS_SDK_INSTRUMENTATION_IMPLEMENTATION_SUMMARY.md`


# Prompt: Set up OpenTelemetry SDK instrumentation across applications

You are an automated coding agent.

Using these documents as the single source of truth, implement SDK-based instrumentation in each application mapped in the CUPs:

1) SDK Initialisation
- Add OpenTelemetry SDK bootstrap in application start-up per recommended patterns for each relevant application.
- Configure resource attributes (service.name, service.version, environment) based on application naming in the CUPs.
- Enable HTTP, gRPC, SQL and other relevant instrumentations using SDK packages as indicated by the plan/diagrams.

1) Signal Configuration
- Traces: Configure OTLP exporters to the collector. Ensure trace context propagation across services consistent with the architecture diagrams.
- Metrics: Register instruments and views required by the plan; emit CUP/SLI metrics defined in the plan using SDK meters.
- Logs: Integrate application logging with the OTEL log bridge/enrichment and forward via OTLP to the collector as outlined in the diagrams.

1) Custom Spans & Metrics for CUP Endpoints
- Wrap controllers/actions listed in the CUP report with manual spans for key business steps
- Emit counters, histograms, and observable gauges required by the plan for availability, latency percentiles, and error rates.
- Ensure async operations link spans and record completion metrics.

1) Collector & Environment Integration
- Use environment variables and appsettings to configure OTLP endpoints, sampling, and headers consistent with the architecture diagrams.
- Do not hard-code endpoints or credentials; read from configuration sourced by the deployment environment.

1) Validation & Testing
- Add unit/integration tests to verify spans and metrics are emitted for CUP endpoints.
- Validate end-to-end propagation in staging with the collector and AWS backends configured by infrastructure.

1) Documentation
- Update READMEs per repository to describe instrumentation setup, configuration, and runbooks.

Constraints:
- DO NOT commit changes.
- Avoid hard-coded values; derive names, thresholds, and paths from the provided documents and configuration.
- Align implementation strictly with the CUP mappings, plan requirements, and architecture diagrams.

Reflect:
- Are changes relevant to the critical user path?
- Are changes reflect the SLO requirement for the relevant critical user path?