# Azure Data Explorer Configuration for OpenTelemetry Demo
#
# This file contains Azure-specific environment variables.
# Copy this file to .env.azure.local and fill in your values.
# Do NOT commit .env.azure.local to version control.
#
# Usage:
#   cp .env.azure .env.azure.local
#   # Edit .env.azure.local with your Azure credentials
#   docker-compose -f docker-compose.yml -f docker-compose.azure.yml --env-file .env.azure.local up

# =============================================================================
# Azure Data Explorer Connection
# =============================================================================

# ADX cluster URI (e.g., https://oteldemoadx.eastus.kusto.windows.net)
ADX_CLUSTER_URI=https://YOUR_CLUSTER.YOUR_REGION.kusto.windows.net

# ADX ingestion URI (for queued ingestion)
ADX_INGESTION_URI=https://ingest-YOUR_CLUSTER.YOUR_REGION.kusto.windows.net

# ADX database name
ADX_DATABASE=otel_demo

# =============================================================================
# Azure Service Principal Authentication
# =============================================================================

# Azure tenant ID (from Azure Portal > Azure Active Directory > Overview)
AZURE_TENANT_ID=YOUR_TENANT_ID

# Service Principal client ID (Application ID)
AZURE_CLIENT_ID=YOUR_CLIENT_ID

# Service Principal client secret
AZURE_CLIENT_SECRET=YOUR_CLIENT_SECRET

# =============================================================================
# OpenTelemetry Collector Configuration (Azure)
# =============================================================================

# Use the Azure-specific collector configuration
OTEL_COLLECTOR_CONFIG=./src/otel-collector/otelcol-config-azure.yml
OTEL_COLLECTOR_CONFIG_EXTRAS=./src/otel-collector/otelcol-config-extras.yml

# =============================================================================
# Grafana Configuration (Azure)
# =============================================================================

# Install ADX plugin for Grafana
GF_INSTALL_PLUGINS=grafana-azure-data-explorer-datasource

# =============================================================================
# Optional: Disable Local Backends
# =============================================================================
# When using ADX, you may want to disable local backends
# Uncomment to disable (handled by docker-compose.azure.yml)

# DISABLE_JAEGER=true
# DISABLE_PROMETHEUS=true
# DISABLE_OPENSEARCH=true
