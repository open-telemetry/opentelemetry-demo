# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# OpAMP collector additions: connect the Collector to the demo OpAMP server
# so it reports health, attributes, version, and effective configuration.
# Loaded when running with compose.observability.yaml.

extensions:
  opamp:
    server:
      ws:
        endpoint: wss://opamp-server:4320/v1/opamp
        tls:
          insecure_skip_verify: true
    instance_uid: 00000000-0000-7000-8000-000000000001
    capabilities:
      reports_effective_config: true
      reports_health: true
      reports_available_components: true
    agent_description:
      include_resource_attributes: true
      non_identifying_attributes:
        deployment.environment.name: demo
        demo.collector.role: gateway

service:
  extensions: [opamp]
