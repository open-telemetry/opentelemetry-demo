{
  "$schema": "https://raw.githubusercontent.com/open-feature/cli/refs/heads/main/schema/v0/flag-manifest.json",
  "flags": {
    "productCatalogFailure": {
      "defaultValue": false,
      "description": "simulates a potential product catalog failure scenario",
      "flagType": "boolean"
    },
    "productCatalogLockContention": {
      "defaultValue": false,
      "description": "simulates lock contention on the product catalog database",
      "flagType": "boolean"
    }
  }
}
